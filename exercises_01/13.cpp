/* 
	Program that:
		Escriba un programa que calcule la distancia más corta de un punto 
		a una línea recta.
*/

#include <cmath>
#include <iostream>

int main (void) {
	double point_x = 0.0;
	double point_y = 0.0;
	double slope = 0.0;
	double intersect = 0.0;

	std::cin >> point_x;
	std::cin >> point_y;
	std::cin >> slope;
	std::cin >> intersect;
	
	

	return 0;
}
